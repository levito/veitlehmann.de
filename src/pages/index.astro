---
import Layout from 'src/layouts/Layout.astro'
import { getContent } from 'src/utils'

const title = 'Veit Lehmann - Senior Frontend Entwickler'
const { sections } = await getContent()
---

<Layout title={title} sections={sections} />
