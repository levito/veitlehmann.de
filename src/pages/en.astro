---
import Layout from 'src/layouts/Layout.astro'
import { getContent } from 'src/utils'

const title = 'Veit Lehmann - Senior Frontend Developer'
const lang = 'en'
const { sections } = await getContent(lang)
---

<Layout title={title} lang={lang} sections={sections} />
