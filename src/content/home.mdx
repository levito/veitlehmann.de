import Link from '../components/Link.astro'
import { getContent } from '../utils.ts'

export const Content = async () => {
  const { links } = await getContent()
  return (
    <p class="section__intro">
      Hallo, ich bin Veit Lehmann, <Link id={1} text={links[1]} /> mit einer
      Passion für <Link id={2} text={links[2]} />,{' '}
      <Link id={3} text={links[3]} /> und <Link id={4} text={links[4]} />.
    </p>
  )
}

export const Footer = async () => {
  const { links, year } = await getContent()
  return (
    <footer class="footer">
      Copyright &copy; {year} Veit Lehmann &middot;{' '}
      <Link id={5} text={links[5]} />
    </footer>
  )
}
