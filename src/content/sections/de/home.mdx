import Home from 'src/components/Home.astro'
import Link from 'src/components/Link.astro'
import { getContent } from 'src/utils'

export const Content = async () => {
  const { links } = await getContent()
  const link = (id) => <Link id={id} links={links} />
  return (
    <Home footerLink={() => link(5)}>
      Hallo, ich bin Veit Lehmann, {link(1)} mit einer Passion für {link(2)},{' '}
      {link(3)} und {link(4)}.
    </Home>
  )
}
